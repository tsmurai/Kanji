# 漢字添削アプリ プロジェクト構想

## 目的
中学受験の苦手漢字を、iPadで繰り返し手書き練習し、とめ・はね・払い・画数の誤りを自動チェックするアプリ。

## 利用の流れ
1. エクセルの苦手漢字リストから出題
2. iPad画面に手書きで解答(Apple Pencil / 指)
3. その場で判定結果を表示
4. 間違えた漢字は優先的に再出題

## 役割分担
| 役割 | 誰 | 場所 | やること |
|---|---|---|---|
| 開発 | 本人 | イギリス | Windows PC + Claude Code |
| 漢字リスト更新 | 妻 | 日本 | ブラウザでエクセルをGitHubにドラッグ&ドロップのみ(Git知識不要) |
| トライアル利用 | 本人 | イギリス | 自分のiPad(お試しユーザーとして) |
| 本番利用 | 息子 | 日本 | 自分のiPad(息子用ユーザーとして) |

## 技術構成
- **開発環境**: Windows PC, Claude Code
- **データ入力**: エクセル(苦手漢字リスト) → JSON変換
- **お手本データ**: KanjiVG(オープンソースの漢字ストロークデータ)
- **アプリ形式**: Webアプリ(PWA)。iPadのホーム画面に追加してアプリのように起動
- **公開・配布**: GitHub Pages(無料プランのため公開リポジトリ)
- **リスト更新の自動化**: エクセルをGitHubにアップロード → GitHub Actionsで自動的にJSON変換 → 数分で反映
- **通信**: 判定はすべて端末内で完結。オフライン対応
- **ユーザー管理**: アプリ内でユーザー切り替え(例:「お父さん(テスト)」「息子」)。進捗データはユーザーごとに分離
- **進捗データの保存**: 各iPad内にローカル保存のみ(クラウド同期なし)

## 判定ロジック(書き順は対象外)
手書きは canvas 等で座標の軌跡データ(点の連続、時間、筆圧)として記録し、以下の3段階で判定する。

1. **画数不足の検出**(簡単・確実): 書いたストローク数とお手本の画数を比較
2. **抜けた画の特定**(やや複雑): 書いたストロークとお手本の各画を対応づけ(マッチング)、対応が見つからない画を「抜けている」と判定
3. **形の正確さ**(最も複雑): 対応づいたストロークペアごとに、終点付近の速度・筆圧変化からとめ・はね・払いを判定

## セキュリティ・プライバシー
- **GitHubリポジトリは公開(Public)**。無料プランでGitHub Pagesを使うため。URLを知っていれば誰でもリポジトリの中身を見られる前提でファイルを置くこと
- エクセルのファイル名・シート内に本名・学校名など個人が特定できる情報を含めない(例:「kanji_list」のような無機質な命名にする)
- 進捗データは各iPadのlocalStorageのみに保存し、リポジトリには入れない

## 実装済み
- [x] エクセルの列構成の確認(番号・問題・答え・結果)
- [x] エクセル → JSON 変換(`tools/excel_to_json.py`)
- [x] KanjiVGデータの組み込み(`tools/fetch_kanjivg.py`、`kanjivg/`)
- [x] 出題・手書き入力UIの実装
- [x] 判定ロジック(画数不足 → 抜けた画特定 → とめ・はね・払い)の実装
- [x] ユーザー切り替え・ローカル進捗保存の実装
- [x] GitHub Pagesへのデプロイ
- [x] GitHub Actionsによるエクセル→JSON自動変換(`.github/workflows/update-data.yml`)

## 次のステップ
- [ ] エクセルの `P6-17` / `P18-28` / `P29-39` / `P40-50` シートは答えの列が空のため未取り込み(各180問)。運用方法を決める
- [ ] 判定精度(とめ・はね・払い)を実際の手書きデータで検証・調整
- [ ] iPad実機でのApple Pencilの書き味・画面サイズの確認
